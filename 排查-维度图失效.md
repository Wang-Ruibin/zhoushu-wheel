# 维度图失效 · 排查记录（插入到 交接文档.md 的待办①之前）

> 背景：新对话完成了「清理死代码 + 拆分 JS 模块 + 修自动弹窗」，之后**属性维度图打不开/不显示**。

## 已核实（这些都没坏）

| 检查 | 结果 |
| --- | --- |
| 维度图绘制链仍在 `index.html` | ✓ `viewChart` L1498 / `drawRadar` L1288 / `drawDharmaWheel` L1210 / `startChartAnim` L1420 / `chartCv` L1511,L1991 |
| 绘制用到的辅助函数都有定义 | ✓ `shadeHex` `WHEEL_INKS` `wheelInk` `wheelInkKey` `CHART_INKS` `chartInk` `brassBall` `dharmaWheelCanvas` `chartRadiusRatio` `GRADE_BASE` `CHART_MAX` `chartDims` `grownLabel` `lastLabelOf` —— **全部在 index.html 里** |
| 模块桥 `window.ZW` | ✓ `js/_ns.js` `ZW.version='v09-11-mod1'`；`util.js`(工具)、`icons.js`、`sound.js`(含 base64 音效)、`spin.js`(旋转+物理效果) 都 `Object.assign(ZW,…)` 挂上了 |
| `index.html` 的转发 | ✓ L72-80 `const { TAU, FONT, WFONT, PALETTES, $, $$, esc, uid, num, norm, clamp, …, spin, animateSpin, applySpinFx, …, loadGearSfx, playGearSfx, gearTick, … } = ZW;` |
| 新测试台 `_dev/run.js` | ✓ 42 项全过（色板/音效/spin/历史/流程/角度共享）—— **但里面没有任何一项检查维度图** ✗ |

**结论：不是"函数被删了"，而是图表的渲染路径在运行时出错了，而测试台没覆盖到它。**

## 最可能的三个原因（按概率排序，逐一验证）

### ① 图表相关符号被模块化后没转发（概率最高）
`startChartAnim` / `viewChart` 现在会调用**已经从 index.html 移到模块里**的东西：
`loadGearSfx` / `playGearSfx` / `gearTick`（→ `js/sound.js`）、`spin` / `applySpinFx` / `SPIN_FX`（→ `js/spin.js`）。
它们在 L72-80 已转发 ✓，**但如果你在图表代码里还用了别的、没进转发清单的模块符号**（例如 `core.fxReduced`、`useSpinHooks` 之外的），运行到那里就 `ReferenceError` ✗。
→ **验证**：浏览器 F12 控制台打开维度图，看红色报错第一行；或在 `_dev/` 里加一项「打开维度图」的测试。

### ② `chartDwellSec()` 的 TDZ / 常量顺序（次高）
`index.html` L86-93：

```js
function chartDwellSec(v){ … clamp(n, CHART_DWELL_MIN, CHART_DWELL_MAX) }
```
注释写着「放在最前面是因为 normalize() 会用到它」。如果 `CHART_DWELL_DEF/MIN/MAX` 是 `const` 且**声明在它下面**，那 `normalize()` → `chartDwellSec()` 会在初始化前访问常量 → **TDZ 报错**（`Cannot access 'CHART_DWELL_DEF' before initialization`），会让含维度图设置的存档一加载就炸 ✗。
→ **验证**：搜 `CHART_DWELL_DEF` 的声明行号，和 L86 比大小（声明必须在前面）；或直接在控制台看报错名。

### ③ 画布尺寸为 0 → drawRadar 直接 return
`drawRadar()` 开头有 `const w = cv.clientWidth; if (!w || !h) return;` —— 如果拆分后 `renderSheet()` 里那段
`requestAnimationFrame(() => { const cv = $('#chartCv'); if (cv && dims.length) startChartAnim(cv, dims, from); })`
被改动/顺序变化，导致动画在**面板还没布局完**时启动，就会静默不画 ✗（面板打开但一片空白，正是用户描述的现象 ✓）。
→ **验证**：控制台 `document.getElementById('chartCv').clientWidth`，若为 0 就是这个。

## 建议的修复顺序（给新对话）

1. **先在浏览器复现并抓报错**（F12 → Console → 打开维度图），拿到确切报错行 —— 比猜快得多
2. 若是 ① → 把缺的符号加进 `index.html` L72-80 的转发清单（或在 `js/_ns.js` 注释里同步维护清单）
3. 若是 ② → 把 `CHART_DWELL_*` 三个常量移到 `chartDwellSec()` 之前
4. 若是 ③ → 把 `startChartAnim` 的启动从 `requestAnimationFrame` 改成一个带尺寸重试的循环（尺寸为 0 时下一帧再试），或改用 `ResizeObserver`
5. **补测试（关键）**：在 `_dev/` 加 `t10_chart.js` —— 造 4 个属性盘 → 建一张维度图 → `viewChart()` 断言含 `chartCv` → `drawRadar(cv, dims, 1, null, 0)` 不抛错 → `startChartAnim` 跑 1.5 秒后 `chartCv.width > 0`。
   这次的回归就是因为**测试台完全没覆盖维度图**才漏过去的 ✗ 以后每加一个功能都补一项。

## 备注
- `_dev/` 不会被 `index.html` 加载，删掉也不影响双击打开 ✓ 保留它，改完就跑 `node _dev/run.js` ✓
- 交接文档里那句「⚠️ 维度图自动弹出/收起未验证」现在可以更新为：**自动弹窗功能已由新对话实现，但伴随出现"图不显示"，先修这个**
