/* ============================================================================
   咒术转盘 · 命名空间
   —— 必须是第一个加载的 js 文件（index.html 里第一个 <script src>）。
   为什么不用 ES module：要双击 index.html 直接打开（file:// 协议），
   而 file:// 下 type="module" 会被浏览器的同源策略挡掉。所以用普通 script + 一个全局对象。
   ============================================================================ */
(function(){
'use strict';
var ZW = window.ZW || (window.ZW = {});
/* version 用来在控制台确认「现在跑的是哪一版」，改代码时顺手 +1 */
ZW.version = 'v09-11-mod1';
})();
