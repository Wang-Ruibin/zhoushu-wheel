# 咒术转盘 · 交接文档（给下一个对话）

> 把这份文件贴给新对话，或直接说「读 交接文档.md 然后按里面的『下一个任务』开工」。
> 本次会话已按「上一版交接文档」做完第 ①②③ 项并核对过代码状态。

---

## 0. 一句话摘要

单文件风格的**手绘 / 纸张质感转盘抽奖器**，双击 `index.html` 即用、离线、无外部依赖、无需打包工具；
支持多转盘与分支流程、等级属性体系、**成长机制**、以及「**属性维度图**」（法轮背景 + 雷达图）。

**运行环境**：Windows，Edge/Chrome，双击 html（`file://` 协议）。
⚠️ `file://` 下不能用 `fetch` 读本地 JSON、不能用 ES module —— 这两条决定了整个架构。

---

## 1. 文件结构（现状 · 已完成 JS 模块化）

```
咒术转盘/
├─ index.html        ~117 KB  入口 + HTML 结构 + 主逻辑（内联 <script>）
├─ app.css            29 KB   样式
├─ index.html.bak    210 KB   模块化之前的备份（确认几天后可删）
├─ README.md          27 KB   用户向功能说明
├─ 交接文档.md                 ← 本文件
├─ 排查-维度图失效.md          用户写的排查记录（里面的事已做完，留档）
└─ _dev/                      开发/调试用（网页不加载，删掉也不影响）→ 见 _dev/README.md
    ├─ README.md              怎么跑回归、harness 怎么用
    ├─ run.js                 ★ 一键回归：node _dev/run.js
    ├─ harness.js             Node 沙箱（桩 DOM/canvas/时钟/数据文件加载）
    ├─ actions.js             死 action 检查
    ├─ csslint.js             CSS 结构检查（括号/孤立声明块）
    └─ t4 … t15                      各主题测试（共 337 条断言）
├─ js/                        插件（普通 <script>，按 index.html 里的顺序加载）
│   ├─ _ns.js      0.7 KB    建 window.ZW 命名空间（必须第一个）
│   ├─ util.js     4.4 KB    $ $$ esc uid num norm clamp / 色板与颜色 / PALETTES
│   ├─ core.js     1.7 KB    跨模块共享的运行时变量（rot / spinning / fxDur …）
│   ├─ icons.js    2.4 KB    svg / ICONS / icon
│   ├─ sound.js     29 KB    音效与震动（含 25KB base64 齿轮音）
│   ├─ spin.js      10 KB    setResult / SPIN_FX 物理效果 / animateSpin / spin
│   └─ multi.js      7 KB    多面转盘：条件判定 + 选面（注入 parseGrade/grownLabel，不认识 state）
└─ 转盘/                      每个转盘一个数据文件
    ├─ _index.js              window.__ZW_INDEX = ["01-xxx.js", ...]
    └─ 01-穿越后的时间点.js …  window.__ZW_REGISTER({...})
```

### 1.1 模块化的三条规矩（**改代码前必读**）

1. **加载顺序 = 依赖顺序**，写在 `index.html` 的 `<script src>` 里。
   `_dev/harness.js` 严格照这个顺序加载（不是按文件名排序），并在顺序不对时直接报错。
   顺序错了的典型症状：`ZW.norm is not a function`（某个函数在别的文件里还没挂上）。
2. **模块之间只走 `ZW.xxx`，不碰别人的内部变量**。
   主逻辑（`index.html` 里的 IIFE）用 `const { ... } = ZW;` 把插件里的东西转成短名，
   所以下面的业务代码写法和拆分前一模一样（`$` / `esc` / `clamp` / `colorOf` …）。
   加了新的 js 文件 → 在文件里 `Object.assign(ZW, {...})`，再到 `index.html` 顶部补一行转发。
3. **模块里需要存档数据 / 界面状态时走「注入」**，不要让插件直接读 `state`：
   - `ZW.usePalette(() => state.settings.palette)`　色板（`util.js` 的 `colorOf` 用）
   - `ZW.useAudioConfig({ sound, vibrate })`　音效 / 震动开关（`sound.js` 用）
   - `ZW.useSpinHooks({ ... })`　旋转模块的十几个依赖（`spin.js` 用，在 `index.html` 结尾注入）
   - `ZW.dom = { canvas, ctx, resultEl, wheelWrap }`　DOM 引用（`spin.js` 用）
   这样注入是因为**插件加载时主逻辑还没跑**（`ZW.dom` 都还不存在），所以取 DOM 要用
   `const resultEl = () => ZW.dom.resultEl` 这种取值函数，不能解构。

### 1.2 数据文件格式

纯 JSON + 一行注册调用（因为 `file://` 不能 fetch JSON 但能 `<script src>`）：

```js
window.__ZW_REGISTER({
  "order": 17,                     // 顺序（网页改顺序会写回这里）
  "name": "是否有特殊天赋",
  "next": "等级评定",               // 默认下一站（写转盘名 / "结束" / "留在本盘" / 空=跟列表顺序）
  "branch": true,                  // 分支转盘：不进默认顺序
  "grade": false,                  // 属性盘开关：true 强制 / false 排除 / 不写=自动识别
  "removeAfterPick": false,
  "options": [
    { "label": "是", "weight": 2, "next": "特殊天赋是" },   // weight 缺省 1
    { "label": "否", "weight": 7 }
  ]
});
```

---

## 2. 功能清单

### 2.1 转盘与抽奖
- Canvas 2D 手绘风转盘：DPR 自适应、径向文字（楷体）、扇区配色自动对比、权重决定扇区角度
- 旋转：easeOut 缓出、指针固定 12 点、实时跟随指针显示扇区名、滴答音 + 中奖音 + 震动
- **点转盘或按空格**开始旋转（原来的「点击旋转」按钮已删除）
- **6 种旋转物理效果**（设置里选）：平稳 / 颠簸（默认，落地轻微压扁）/ 2D滚筒（平面绕圈）/ 3D滚筒（透视 + 倾角轴绕圈）/ 收势
  - 参数：`FX_2D_TURNS_PER_SEC=1.5`、`FX_3D_TURNS_PER_SEC=1.1`（都在 `js/spin.js` 顶部）
- 结果面板：复制、去下一站、再抽一次

### 2.2 转盘管理（「转盘」页）
- 增删改名、复制、↑↓ 排序、**按住拖动排序**（鼠标拖 >5px / 触屏长按 320ms）
- **点整行**进该转盘的「选项内容」页；右下角功能按钮不受拖动影响
- 「分支」标记：不进默认顺序、只能被选项跳转指到
- 「属性」标记：自动识别为属性盘（选项里 60% 以上是等级标签）
- 顺序写进数据文件**三处**：文件名 `01-` 前缀 + 每个文件的 `"order"` + `_index.js` 清单注释
- **维度图行**也在这里：显示位置文字 + ↑↓ 调位置 + 查看/删除

### 2.3 选项内容页
- 批量编辑（覆盖/追加，支持 `甲*3` 权重语法）
- 每行：内容 / 权重 / 颜色 / 跳转目标（`→`）/ 拖动排序
- 顶部：**默认下一站**（点开选目标）、计入属性维度图开关、抽中即移除

### 2.4 分支流程
- 三级优先级：**选项级 `next` > 转盘级 `next` > 列表顺序兜底**
- 分支转盘转完：接回「来路转盘」的默认顺序；原盘没设 → 顺列表顺序
- 防死循环：最近 12 步内出现长度 1~4 的循环重复 3 轮 → **临时暂停**（可一键继续）
- 流程条：路径胶囊 + 「→ 下一站」/「⏸ 已暂停」按钮 + 起点重来
- 「默认下一站=结束但后面还有转盘」→ 「转盘」页出朱砂提示

### 2.5 属性 / 等级 / 成长
- 等级解析：`E- E D C B A S SS SSS EX`（含 `+/-`，大小写都认），分值 = 等级 × 10
- **刻度**：`E-(17) = 维度图 0 位置`，SSS(90) = 满格，**EX(100) = 溢出一格(1.2R)**
- **成长机制**：抽到 `体术+1` 这类 → 对应属性升 N 级，阶梯封顶在 EX；升级后保留该盘自己的括号说明
- 两个特殊：`虚度光阴（无属性成长）` 不成长；`学会反转术式` 不误判
- 累计在 `state.flow.grow = { wheelId: steps }`，**重新开始一轮清空**

**★ 加新的属性成长要不要改代码？（用户问过）答：不用。**

成长文字的解析在 `parseGrowth()` / `growTargetWheel()`，匹配顺序是：

```
① 转盘**全名**相等                     （「剑术造诣+1」）
② 转盘名**包含**这个词                 （「剑术+1」→ 剑术造诣、「造诣+1」也行）
③ 内置别名表 GROW_ALIAS               （「体术」→ 体术水平、「咒操」→ 咒力操纵 …）
```

只要**成长词能对上牌组里某个转盘的名字**（部分匹配也算）就自动生效 ——
**不需要改代码，也不需要维护别名表**。别名表只是给"简称跟全名没有子串关系"的情况兜底。

支持的写法（`_dev/t15_newgrowth.js` 里逐条测着）：
```
体术+1                        简称（走别名表）
咒力操纵+1 / 剑术造诣+1        全名
剑术+1 / 造诣+1               名字的一部分
体质（基础体质）+1             写全名、**带括号**也行（本轮才补上）
我悟了！（咒操与效率+1）        括号里的补充说明；「与/和/、」隔开的多目标共用 +N
我悟了！（阴阳术+1）（剑术+2）  多个括号各算各的
```

**唯一会"安静地不生效"的情况**（不报错，只是转了白转）：
| 情况 | 说明 |
| --- | --- |
| **词对不上任何转盘** | 写错字 / 目标盘被删了。**最常见** |
| **有歧义** | 「咒力+1」同时能匹配「咒力总量」和「咒力操纵」→ 取**列表里靠前**的那个；写全名可避免 |

⚠️ 为了不再"安静"，**「选项内容」页底部会用朱砂提示点出对不上的写法**
（`growHintBlock()` / `growHintFor()`）—— 写错字当场就能看到。

**另外几条**（不是 bug，但要知道）：
- 名字再长也能命中（正则抓「+ 前最多 `GROW_WORD_MAX = 24` 字」，而那段总是名字的子串）
- 名字里有空格/括号也会在分隔符处截断，截出的前缀仍是子串 → 照样命中
- **数字型递增靠"从标签里提取数字"**：`1个 → 2个` 可以；
  `没有 / 有几个 / 很多` 这种**没有数字**的不会递增（想让它长就写数字，或用等级标签）
  · 注意 `不会` 被当成 0，所以 `不会 +1 = 1个`
- 数字型 / 文字型的属性盘**不进维度图**（维度图只看等级标签）
- 成长**会**传到维度图：`isGradeWheel()` 和 `grownLabel()` 都按「当前展示的选项」算

### 2.6 属性维度图（= 转盘的变体：转盘产数据，维度图统计数据）
- 新建：在「记录」页或「转盘」页；选**位置**（列表最前 / 某个转盘之后 / 所有转盘之后）
- **数据是打开那一刻按当前进度实时统计的**（不是定格快照）：
  - 位置=最前 / 最后 → 统计**本轮到目前为止抽过的全部**
  - 位置=某个转盘之后 → 只统计**它之前（含自己）**的属性盘，没抽到显示 `—` / 0
  - 范围里没有属性盘（放在很靠前）→ 退回本轮全部
- **★ 成长会体现在图里**（两张图走的是**两条不同的代码路径**，改的时候两个都要顾）：
  | 位置 | 数据来源函数 | 施加成长 |
  | --- | --- | --- |
  | 某个转盘之后 | `dimsUpTo(anchorId)` | ✅ |
  | 最前 / 所有转盘之后 | `chartDims()` | ✅（**这里曾经漏了**，见 4-⑩） |

  成长累计在 `state.flow.grow = { wheelId: steps }`，读取时经 `grownLabel()` 叠加，
  沿阶梯 `E- E D C B A S SS SSS EX` 上移，**EX 封顶**；升级后保留该盘自己的括号说明
  （`C` → 该盘里的 `C（普通）`；该盘没有这一档时退回裸等级名）。
- 面板：**只有图名 + 图 + 关闭按钮**；标签只写「转盘名 + 等级」
- **法轮背景**（几何严格对应）：8 根辐条、外环压在 **SSS**、外圈 8 颗小球圆心在 **EX(1.2R)**、中心球
  - **离屏合成**：先在离屏画布 100% 画好，再整体按一次 `globalAlpha` 贴上（`settings.wheelAlpha`，默认 0.40）
  - 颜色：`WHEEL_INKS` = 六色 + 素白，与维度图颜色分开设置
- 转动：**逆时针 45°**，分 8 格；分割点 `GEAR_TURN_AT = 0.665`（由音频包络分析得出）
- 音效：B 站「魔虚罗音效素材」BV1KF3S62EaK 的 0~3 秒 → 18KB mp3 base64 内嵌（`gearTick` 是合成音兜底）

### 2.7 ★ 多面转盘（本次新加）

**一句话**：照常参与默认顺序、位置随便放的普通转盘，但**展示什么内容取决于之前某个转盘抽到了什么**。

**和分支转盘的区别（最容易混，代码里也分得很清）**：

| | 分支转盘 `branch` | 多面转盘 `multi` |
| --- | --- | --- |
| 影响什么 | **流程跳到哪** | **这个转盘自己显示什么** |
| 默认顺序 | 不进（只能被选项跳转指到） | **照常参与**，位置随便换 |
| 实现 | `flow.retStack` 记住来路 | `faceOf()` 挑一个「面」 |

**数据格式**（存进 `转盘/*.js`，也可以手改）：

```js
window.__ZW_REGISTER({
  "name": "声望",
  "multi": true,
  "faceBy": "道德水平",          // 按哪个转盘的结果分面（写转盘名；空 = 永远用兜底面）
  "faces": [                      // 从上往下找**第一个命中**的
    { "when": { "type":"gradeAtLeast", "value":"A" }, "options": [
        { "label":"EX（超凡入圣）" }, { "label":"S（家喻户晓）" } ] },
    { "when": { "type":"gradeAtMost",  "value":"B" }, "options": [
        { "label":"EX（婴儿止啼）" }, { "label":"S（臭名昭著）" } ] }
  ],
  "options": [ { "label":"（兜底）默默无闻" } ]    // 都没命中时用它
});
```

**6 种条件**（`when.type`）：

| type | 界面上的字 | 说明 |
| --- | --- | --- |
| `gradeAtLeast` | 等级 ≥ | 按阶梯 `E- E D C B A S SS SSS EX` 比 |
| `gradeAtMost` | 等级 ≤ | 同上 |
| `gradeIs` | 等级 = | 正好这一档（`+/-` 会取整到同一档） |
| `labelIs` | 结果 = | 抽到的文字完全一样 |
| `labelHas` | 结果包含 | 抽到的文字里含这几个字 |
| `else` | 其他情况 | 一般放最后一条当"否则" |

**几个关键行为**（都有测试盯着）：
- **判定用「叠加成长之后」的结果** —— 成长把道德从 B 抬到 A，声望就切到高道德那一面
- 按 `faces` 顺序取**第一个**命中的 → **顺序 = 优先级**
- 那个盘**还没抽过** → 等级类条件一律不命中 → 落到兜底面
- `path` 里找不到时**回退到 `history`**（分支流程会截断/重走 path，不至于失忆）
- 兜底面既能编辑、也是"普通转盘"的那套 `options` —— 可以先填内容再加条件

**界面上在哪操作**：
| 位置 | 做什么 |
| --- | --- |
| 「转盘」页行里的「**多面**」标记 | 点一下变多面（会自动猜一个分面依据）；再点一下恢复普通（会确认） |
| 「选项内容」页顶部的多面提示条 | 看清**正在编辑哪一面**；`自动 / 第1面 / 第2面 / 兜底面` 切换 + 「管理面 / 条件…」 |
| 「面管理」面板 | 改分面依据、加面/删面、面排序（↑↓ = 调优先级）、改条件、编辑某一面的内容、编辑兜底面 |
| 「条件编辑」页 | 选 6 种条件类型之一；等级类给出 `E-`～`EX` 全部档位，文字类从依据转盘的选项里挑（也能手写） |
| 流程条 | 当前转盘是多面盘时，额外标出它此刻用的是哪一面 |

**实现要点**（改的时候别踩）：
- **唯一收口是 `wheelOptions(w)`**：所有"要显示 / 要抽"的地方都走它，别直接读 `w.options`
  （否则多面转盘在那些地方会显示成兜底面）。编辑界面走 `faceOptions(w, index)` / `editOpts(w)`。
- **按 id 找选项一律走 `findOptionById(w, id)`**：兜底面 + 每一面都扫。
  ⚠️ 这条踩过一次狠的：只查 `w.options` 的话，**面里的选项在"输入"路径上会被静默丢弃**
  （`if (!o) return`，不报错、内容就是不保存）。同类地方还有 6 处，已一并改掉，见 4-⑫。
- `isGradeWheel()`、`growLabel()` 也改成看 `wheelOptions()` —— 不然多面转盘/成长定位会按兜底面算错。
- `faceBy` **存 id 也认名字**：内存里是 id（界面选出来的），手写文件里是名字。
  `js/multi.js` 的 `byRef()` 两种都认，主逻辑用 `faceSource(w)` 包了一层 —— 别各处分别写 `wheelById`。
- 判定逻辑全在 **`js/multi.js`**（注入 `parseGrade` / `grownLabel` / `flowPath` / `history` / `wheels`），
  不认识 `state`，所以能单独测。



流程走到「某转盘之后正好有一张维度图」时：

```
抽完 → 等 650ms（CHART_SHOW_DELAY，让结果动效播完）
     → 弹出维度图 + 提示条「本站属性维度图 · N 秒后继续 [现在继续]」
     → 停留 chartDwell 秒（默认 3.4）
     → 自动收起 → 等 280ms（CHART_RESUME_GAP，等收起动画）
     → 继续跳到下一站（continueFlow）
```

**三种"用户插手"的情况都不会把流程卡住**：
- 用户提前按关闭 / Esc → 走 `afterChartDismiss()`，流程照常继续
- 弹出时用户已经开着别的面板 → 不抢焦点，流程直接交给下一站
- 停留期间用户自己换了转盘 → 收图后**不**把用户拽回来，只清掉 `pendingId`

**设置项**（都在「设置」页的「流程」段）：
| 设置 | 默认 | 说明 |
| --- | --- | --- |
| `chartMid` | true | 「中途走到维度图时弹出来看一眼」—— 中途弹图的开关 |
| `chartDwell` | 3.4 秒 | 「维度图停留」滑杆（1.0 ~ 15 秒，`data-field="flowDwell"`） |
| `chartOnEnd` | true | 「流程结束时展示属性维度图」—— 只管**结束**时那一张 |

⚠️ 中途弹图和结束弹图现在是**两个独立开关**（以前共用 `chartOnEnd`，一关就两个都没了）。
⚠️ 手动模式（`autoJump=false`）或已暂停时**不**自动弹图 —— 免得抢注意力。

### 2.8 数据与设置- **文件双向同步**：授权一次目录后，改动 900ms 自动写回 `转盘/*.js`（含删掉废弃文件）
- **「从文件重新读取」**：用文件覆盖网页（会先确认）
- 加载优先级：**浏览器缓存为准**，文件只在「第一次打开」或主动重读时生效
- 设置：6 套调色板、深浅主题、旋转时长、音效、震动、标题跟随指针、文字自动翻转、历史条数、
  流程开关（使用分支流程/自动跳转/停留时长/跳转后自动旋转/中途弹图/维度图停留/结束时展示维度图/起点/清除所有连接）、
  旋转物理效果、维度图颜色、法轮颜色 + 透明度、数据导入导出/复制/恢复默认

### 2.9 其他细节
- 扇区文字排版：单行放得下且字号够看 → 单行；会被压到看不清 → **自动两行**；两行也塞不进 → **不截断**（允许被转盘遮住）
- 页面层级：单一中枢 + **真返回键**（`NAV.stack` 栈顶=当前状态 + History API + `swallowPop`）
- 启动小字提示：`点转盘开始 · 点结果看操作 · 空格也行`

---

## 3. 代码思路与关键标识

### 3.1 整体结构

```js
// index.html 里是**一个 IIFE**，内部所有状态与函数共享闭包作用域
(function(){
  const { ...一堆短名 } = ZW;        // ← 命名空间转发（插件里的东西）
  // 常量：BASE_WHEELS / V3_PRESETS / BASE_SETTINGS / BASE_FLOW / STATE_VER / CHART_*
  // 状态：state = { version, currentId, wheels[], charts[], settings{}, history[], lastChart, flow{} }
  // 视图：ui = { view, mtab, flowCollapsed, targetCtx, chartDims, chartFrom, chartItem, lastSync… }
  // 导航：NAV = { stack, swallowPop }  navTo/navBack/navGoBack/navCloseAll  topNav/navDepth
  // 绘制：draw() → layoutSectors → 扇区/文字/中心；drawRadar → drawDharmaWheel
  // 交互：bindEvents() 用事件委托（sheet 上 data-act）
  // 注入：useSpinHooks({...})  →  init() → loadWheelFiles(defs) → boot()
})();
```

**跨模块共享的东西**在 `ZW.core`（`js/core.js`）：`rot`（弧度）、`spinning`、`drawQueued`、
`wheelSize`、`fxDur`、`fxReduced`。用 getter/setter 包了一层，读写手感和普通变量一样。
`rot` 由 `js/spin.js` 每帧改写、主逻辑的 `draw()` 读它 —— 所以两边都写 `core.rot`，
**不要**在主逻辑里再留一个本地 `rot` 副本（会不同步）。

### 3.2 必须知道的坑（踩过的，按痛苦程度排序）

0. **同一个数据有两条代码路径时，改一条忘一条**
   最典型的一次：维度图的数据有 `dimsUpTo()`（位置=某转盘之后）和 `chartDims()`
   （位置=最前/最后）两个函数，只给前者加了成长 → 第二张图永远不涨。
   → **凡是"同一件事有两处实现"，改的时候必须两处都改**，并且**两条路径都要有测试**
     （`_dev/t12_growth.js` 就是这么来的）。多面转盘也有一对：`wheelOptions()` / `faceOptions()`。

1. **拆模块后「名字不在作用域里」——同一个坑踩了两次，两次表现完全不同**
   | 符号 | 它在模块里是什么 | 谁引用了它 | 现象 |
   | --- | --- | --- | --- |
   | `GEAR_SFX` | `js/sound.js` 的顶层 `let` | `startChartAnim()` 的 rAF 回调 | `ReferenceError` 不显示 → **「维度图打不开」** |
   | `fxReduced` | `js/spin.js` 里 `core` 的属性 | `tabSettings()` | `viewManager()` 抛错 → **「设置/记录」面板整个打不开** |

   规律：**跨模块用的每个名字，都要在模块里包一层并加进 `Object.assign(ZW,{...})` + 页面的
   `const { ... } = ZW` 转发清单**。忘了任何一边，运行到那一行才炸，而且往往在
   渲染函数 / rAF 回调里，报错根本看不见。
   → 现在有 `_dev/symbolscan.js` 专门盯这件事（两个方向都查），已接进 `run.js`。
   → **不要导出天量字符串**（25KB 的 base64 挂命名空间上）；包一个 `hasGearSfx()` 这样的小函数。

2. **rAF / setTimeout 回调里的异常是"隐形"的**
   `startChartAnim()` 那次，异常在 `requestAnimationFrame` 回调里，既不冒泡也没人接，
   面板照常打开、画布空白 —— 控制台之外一点线索都没有。

3. **删代码时小心"顺手带走别的函数"**
   删 8 个死 action 时，`tabHistory()` 的定义恰好住在被删的代码块里，被一起删掉了。
   而它只被 `viewManager()` 用字符串一类的调用点引用 —— 静态检查（死 action 检查）看不出来。
   → 教训：**删完要真的把每个视图渲染一遍**（`t11_views.js` 就是干这个的）。

4. **桩（测试替身）的假故障会掩盖真故障**
   `_dev/harness.js` 曾有三处失真（`$()` 查不到 `innerHTML` 里的元素、canvas 没
   `width`/`height`、`<script src>` 的 onload 不触发导致 `boot()` 从没跑过）。

5. **`PALETTES[paletteKey]` —— 取值函数忘了调**
   `usePalette` 存的是**函数**。直接当键名用会 `undefined` → **静默**退回默认色板。

6. **模块加载顺序**（见 1.1）。顺序错了报「某函数 is not a function」。

7. **`rot` 是弧度**，且旋转时每帧变化几十度 → 用 `sin(rot)` 做的视觉要**降频**或按时间驱动。

8. **半透明图形重叠会叠深** → 用**离屏画布合成**。

9. **`.replace()` 只替换第一处** → 涉及 x/y 两个坐标时必须 `split().join()`。

10. **`state.settings` 与 `s`** → `tabSettings()` 里别再写 `s.settings.xxx`。

11. **结果区高度不能用 `em`** → 字号变化会改变高度，flex 居中的转盘会上下跳。

12. **插件加载时主逻辑还没跑** → 插件模块级代码不能解构 `ZW.dom`，要用取值函数。

13. **工具链的坑**：PowerShell here-string 里的 `` `r`n `` 替换会把多行压成一行，
    日志里还会显示成乱码 —— **改正经文件时用编辑工具，别用 shell 拼字符串**。

### 3.3 ★ 改完必做（回归）

```bash
node _dev/run.js
```

五步：内联脚本语法 → `app.css` 结构 → 死 action → **模块符号**（模块私有/忘了转发）
→ 跑 `t4`…`t15`（**共 337 条断言**）。任何一项挂了退出码 1。
细节见 `_dev/README.md`。

**★ 一条规矩**：以后每加一个功能，就在 `_dev/` 补一项测试。
两次栽跟头都是"功能/代码动了、测试没跟"，而当时全绿的断言一条都没碰到出问题的地方。

**浏览器端验证**：
```
localStorage.setItem('zhoushu-wheel.debug', '1')   // 刷新
ZW.__app.openChart()                              // 控制台里直接把图打开
```
（用这个办法已截图确认：维度图正常显示、「记录」页正常渲染，控制台无报错）
⚠️ 无头 Edge 下遮罩面板在虚拟时间下**不会绘制**，所以别看无头截图说"弹窗没出来"。

---

## 4. 本次会话做完的事

### ① 修「维度图自动弹出/自动收起」✅

**上一版交接文档怀疑的「没弹出来」是误判** —— 用 `_dev/harness.js` 一比一复现后，
650ms 弹图、3.4 秒自动收起、接着跳下一站，整条链路本来就是通的。

但顺带挖出**五个真实缺陷**并全部修掉：

| # | 问题 | 修法 |
| --- | --- | --- |
| 1 | 停留时长写死 3400ms | 抽成 `chartDwell` 设置项 + 设置页滑杆 |
| 2 | 中途弹图与结束弹图共用 `chartOnEnd`，关一个两个都没 | 拆成 `chartMid` / `chartOnEnd` 两个开关 |
| 3 | **用户提前关掉维度图 → 流程永久卡住** | 加 `afterChartDismiss()`，两条关闭路径都能恢复流程 |
| 4 | 停留期间用户换转盘，收图后还是被拽到 next | 收图时比对 `state.currentId`，变了就只清 `pendingId` |
| 5 | 自动弹图后提示条一直飘在下一站 | 收起时 `hideToast()` |

### ② 手工清理死 action ✅

共删 **11 个**（文档列了 8 个，另外 3 个是顺手扫出来的）：
`pick openManager openChart copyChart clearSlot toggleNode expandAll chainOrder
 clearHistory copyHistory resetFlow2`

### ③ JS 模块化拆分 ✅

```
js/_ns.js   js/util.js   js/core.js   js/icons.js   js/sound.js   js/spin.js
```
`index.html` **186 KB → 146 KB**。做法是「命名空间 + 依赖注入」而不是 ES module
（`file://` 下 module 会被挡，双击就打不开）。每拆一块跑一次回归。

### ④ ★ 修「维度图不显示」（用户在浏览器里发现）✅

**根因（不是猜的，复现出来的）**：

```
ReferenceError: GEAR_SFX is not defined
    at frame (startChartAnim)
```

`GEAR_SFX`（25KB base64 齿轮音）在拆模块时搬进了 `js/sound.js` 并变成**模块私有**，
但 `index.html` 的 `startChartAnim()` 里还在用它判断"要不要退回合成音"，
而转发清单里没有它。

为什么**看不到任何报错、只看到"图打不开"**：
那一行在 `requestAnimationFrame` 回调里，异常既不冒泡也没人接，
面板照常打开、画布一片空白 —— 表现就是"维度图打不开"。

**修法**：不导出那个 25KB 字符串，而是在 `js/sound.js` 里加一个小函数
`hasGearSfx()` 并转发；`index.html` 改成 `if (!played && !hasGearSfx() && !triedSynth)`。
（把天量字符串挂在命名空间上本来就不该做。）

### ⑤ 为什么测试台漏掉了它 —— 三个桩的洞（都补上了）

用户排查记录里点得很准：**「测试台完全没覆盖维度图」**。实际比这还糟一点，
原来的桩有三处"假故障"，会把真问题盖住：

| 洞 | 后果 | 修法 |
| --- | --- | --- |
| `$('#chartCv')` 查不到动态写入 `innerHTML` 的元素 | `startChartAnim` 根本不会被调用 | 桩改成真的解析 `innerHTML` 并把节点挂进文档树 |
| canvas 没有 `width`/`height` 属性 | `drawRadar` 算不出尺寸 | 补上（默认 300×150，和浏览器一致），`getContext` 会记录绘制调用 |
| `<script src>` 的 `onload` 不触发 | `boot()` **从来不执行**（图标/启动小字/加载数据文件全没跑） | 桩真的从磁盘读 `转盘/*.js` 并执行、触发 onload/onerror |

一句话：**桩的假故障会掩盖真故障**——所以桩要尽量像真的，别图省事。

### ⑥ 新增 `_dev/t10_chart.js`（27 条）—— 补上缺的那一环

和 `t8_draw.js` 的区别是关键：t8 是**直接调 `drawRadar()`**（跳过了整条渲染链），
t10 走**真实路径**：

```
造 4 个属性盘 → navTo('chart') → renderSheet() → viewChart()
→ rAF → startChartAnim() → drawRadar()
断言：面板 HTML 里有 chartCv、查得回 canvas、画布尺寸 > 0、
      ctx 上真的记到了绘制调用（stroke/fill/fillText/drawImage）、
      自动弹图那条路也画出来了、连开两张（变形动画）、老存档也能打开
```

自动化的价值就在这里：这个测试**在修之前就是红的**（0 次绘制调用），
修完变绿 —— 它是真的能挡住这个问题，不是摆设。

### ⑦ 顺手修的两个真 bug（计划外）

1. **`app.css` 有 4 处孤立声明块**（上一轮抽 CSS 时选择器被删、只剩属性）。
   多出来的 `}` 会破坏后续解析，**后面 200 多行样式其实全被浏览器丢了**。
2. **色板解析恒等于「鲜艳」**：`PALETTES[paletteKey]` 把注入的函数当键名了 →
   `undefined` → 静默退回默认色板。由 `t9_modules.js` 看着。

### ⑧ 加了 `ZW.__app` 调试出口（默认关闭）

```
localStorage.setItem('zhoushu-wheel.debug', '1')   // 刷新后
__app.state / ui / openChart() / spin() / flowJump() / canvasCalls()
```
用户排查记录里"按 F12 看报错"那条路是对的；有了这个出口，
下次可以直接 `__app.openChart()` 把图打开，不用手点。

### ⑨ ★ 修「设置 / 记录 按钮点不动」（用户第二轮反馈）✅

**根因有两层，是"拆模块残留"的两种不同形态**：

**第一层（直接原因）：`tabHistory()` 被误删**
删 8 个死 action 时，`tabHistory()` 的定义恰好住在被删的那段代码块里，被一起删走了。
于是 `navTo('manager',{mtab:'history'})` → `viewManager()` → `tabHistory()` →
`ReferenceError` → **整个「设置/记录」面板打不开**（不是"按钮坏了"，是渲染就崩了）。
→ 已从备份恢复，并按现在的位置语义补了说明文字。

**第二层（紧随其后会炸的）：`fxReduced` 没转发**
`tabHistory` 修好之后，切到「设置」还会炸：`fxReduced` 在 `js/spin.js` 里是
`core` 的**属性**（不是模块变量），页面直接当变量用 → `ReferenceError`。
→ 改成 `js/spin.js` 导出 `prefersReducedMotion()`，页面用函数取值。

**为什么 151 项断言全绿却漏了它**：没有一项**真的去渲染面板**。
`t9` 检查了导出清单、`t10` 检查了维度图，但"设置页 / 记录页能不能渲染出来"没人看。

**这一轮新增的防护**：
| 新增 | 作用 |
| --- | --- |
| `_dev/t11_views.js`（27 条） | **把每个 view × 每个标签都渲染一遍**，断言内容非空 + 关键元素都在。弄没 `tabHistory` 立刻红灯（已负向验证过） |
| `_dev/symbolscan.js` | 扫描「模块私有 / 忘了转发」的名字，两个方向都查。删掉任何一处转发立刻报出来（已负向验证过） |
| `_dev/run.js` | 上面两项都接进一键回归了 |

```bash
node _dev/run.js     # 现在共 337 条断言 + 5 项静态检查
```

### ⑩ ★ 修「第二张维度图不体现成长」（用户第三轮反馈）✅

**现象**：成长转盘插在两张维度图中间，抽到「咒力总量+2」之后，第二张图的数值没变。

**根因**：维度图的数据有**两条代码路径**，一条施加了成长、另一条忘了：

| 函数 | 谁在用 | 原状 |
| --- | --- | --- |
| `dimsUpTo(anchorId)` | 位置=**某个转盘之后**的图 | ✅ 有 `grownLabel()` |
| `chartDims()` | 位置=**最前 / 所有转盘之后**的图 | ❌ **直接返回原始标签，漏了成长** |

而 `chartLiveDims()` 对「所有转盘之后」是**直接 return `chartDims()`**，
所以第二张图拿到的永远是没成长过的原始等级。
（顺带：变形动画的起点 `ui.chartFrom` 也走 `chartDims()`，所以起点数据也是错的。）

**修法**：在 `chartDims()` 的 `add()` 里补上 `grownLabel(key, label)`，
和 `dimsUpTo()` 保持同一套语义。已验证（真实浏览器）：成长前 `E（很弱）(20) / D（偏低）(30)`
→ 成长后 `C（普通）(40) / B（还行）(50)`。

**新增 `_dev/t12_growth.js`（24 条）**，覆盖：
- 成长转盘里**哪些选项算成长**（用户特地提醒"这个你自己判断"，逐条断言了）
- 成长后**两张图都要体现**（两条路径都测）
- 多步叠加 + EX 封顶
- 变形动画起点也用成长后的值
- 位置语义没被改坏

> ⚠️ **一个还没动的位置语义问题**（见 5-①）：
> `dimsUpTo()` 用的是「本轮累计成长」，**不区分成长发生在第几步**。
> 所以把图放在成长转盘**之前**，它的数值**也会**跟着涨（实测确认）。
> 要改成"按位置定格"需要给 `flow.path` 每步记下当时的成长快照 —— 属于设计选择，
> 等用户拍板再做。

### ⑪ ★ 新功能：多面转盘（用户第四轮需求）✅

**需求原话**：「类似分支转盘，但参与到转盘的默认顺序中，多面转盘根据之前抽到的转盘结果不同，
自身会展示不同的内容。比如声望转盘：道德 A 级以上则 EX 是超凡入圣、S 是家喻户晓；
B 级以下则 EX 是婴儿止啼、S 是臭名昭著。和分支转盘的区别是，分支转盘由结果直接跳转进入、
必须紧跟在原转盘后；多面转盘顺序随意更换，与普通转盘无区别。」

设计上把两件事彻底分开（数据、UI、代码都分开）：
- **分支 = 流程跳到哪**（`branch` / `flow.retStack`）
- **多面 = 这个盘显示什么**（`multi` / `faces` / `faceOf()`）

新增 `js/multi.js`（判定逻辑，注入 `parseGrade`/`grownLabel`/`flowPath`/`history`/`wheels`，不认识 state），
以及两个面板（面管理 / 条件编辑）和「选项内容」页的切面条。
详细格式和行为见 2.7。

**做的过程中发现并修掉的一个真问题**：`faceBy` 在内存里存 **id**、在数据文件里存**名字**
（和 `next` 一样，方便手改）。如果各处分别写 `wheelById(faceBy)`，
手写文件导进来之后就会"条件都对、却永远走兜底面"——
现在统一走 `faceSource(w)` → `js/multi.js` 的 `byRef()`（id 和名字都认）。

新增 `_dev/t13_multi.js`（71 条），覆盖：6 种条件、逐档边界（E- 到 EX 每个都过）、
多个面命中取第一个、成长改变分面、path 空时回退 history、文件往返（两种 faceBy 写法）、
界面渲染、面管理的动作（加/删/改条件/排序）。

### ⑫ ★ 修「多面转盘里填的内容不见 / 存不住」（用户第五轮反馈）✅

**现象**：在多面转盘的某个面里填内容，还没填完、点去别的页面，**内容就没了**。

**根因**：`bindEvents()` 里有这么一段（sheet 的 input 事件处理器）：

```js
const o = currentWheel().options.filter(x => x.id === id)[0];   // ← 只查兜底面
if (!o) return;                                                 // ← 静默丢弃
```

多面转盘里你正在编辑的那个选项住在 `faces[i].options` 里，`w.options`（兜底面）里**没有**它
→ `o` 是 `undefined` → **直接 return，一个错都不报**。
—— 这是我上一轮自己引入的：**渲染和编辑动作都改成按面取选项了，唯独"输入"这条路径忘了同步。**

**修法**：把"按 id 找选项"收成一个函数，兜底面 + 所有面都找：

```js
function findOptionById(w, id){ … }        // 兜底面 → 每个面，都扫一遍
```

并把同类的地方一起改了（**同一个坑的全家桶**）：
| 位置 | 原来 | 现在 |
| --- | --- | --- |
| `input` 事件（打字） | `w.options.filter(…)` | `findOptionById(currentWheel(), id)` |
| `optById()` | 只查 `w.options` | 内部走 `findOptionById`（**「点 → 设跳转目标」也修好了**） |
| `viewResult()` / `removeWinner` | 只查兜底面 | 按面查，删的时候从**它实际所在的那一组**删 |
| `clearLinks` | 只清兜底面的 `next` | 面里的选项也一起清 |
| `dup`（复制转盘） | **完全丢掉 multi/faces** | 连面 + 分面依据一起复制 |
| 「转盘」页小圆点取色 / 目标选择器的「N 项」 | 按兜底面算 | 按当前展示的选项算 |

**新增 `_dev/t14_faceedit.js`（24 条）**：把每个输入字段都真打一遍字，
然后切 mtab / 关面板 / 重建实例（模拟刷新）逐层确认内容还在；
还覆盖了"两个面互不串味"、「权重/颜色/删除落在当前面」、「面里的选项能设跳转目标」、「批量编辑按面」。
**已负向验证**：把修复退回原样，这个测试立刻红灯。

**教训（和第 4-⑫ 节那条是同一个）**：
> 引入"同一份数据有多层（兜底面 + 若干面）"之后，**所有"按 id 定位对象"的地方都要重查一遍**。
> 光改渲染不够 —— 输入、删除、跳转、批量、复制、清空，每一条路径都得过一遍。
> `_dev/symbolscan.js` 能抓"名字不在作用域"，但抓不到这种"逻辑上漏了一层"，
> **只能靠测试把每条路径都走一遍**。

### ⑬ ★ 回答「新加属性成长，系统还能统计到吗」+ 顺手修好括号写法（用户第六轮提问）

**结论：能，不用改代码。** 成长词只要**对得上某个转盘的名字**（部分匹配也算）就自动生效，
`GROW_ALIAS` 别名表只是给"简称跟全名没有子串关系"的情况兜底，不用维护。

回答的过程中把解析器**实测**了一遍（`_dev/t15_newgrowth.js`，40 条），发现：

| 发现 | 处理 |
| --- | --- |
| **`体质（基础体质）+1` 这种"写全名带括号"解析不出来**（正则跨不过括号） | ✅ 修了：先扫不跨括号的，再补一轮「（…）紧跟 +N」的合并匹配 |
| `我悟了！（阴阳术+1）（剑术+2）` 多个括号只认第一个 | ✅ 修了：现在每个括号各算各的 |
| 24 字的 `GROW_WORD_MAX` 是散在正则里的魔法数字 | ✅ 提成常量 + 注释说明"其实长名字也能命中" |
| 成长词对不上转盘时**完全静默** | ✅ 加了提示：「选项内容」页底部朱砂标出对不上的写法（`growHintBlock()`） |

**我自己的两个错误假设（记下来免得下次又猜）**：
- ❌「名字超过 24 字会解析不到」→ 错。正则抓的是「+ 前最多 24 字」，**那片段总是名字的子串**，
  所以 27 字、40 字的名字照样命中
- ❌「名字里有空格会失败」→ 错。会在空格处截断，截出的前缀仍是子串 → 照样命中
（这两条一开始被我写进了测试期望，红灯之后才去实测纠正 —— **先测再下结论**）

顺带确认的真实现状（不是问题）：
- 数字型递增靠"从标签提数字"，`1个 → 2个` 可以；`没有/有几个/很多` 没有数字所以不递增
- `不会` 当 0 看，所以 `不会 +1 = 1个`（不是 2个）
- 数字/文字型属性盘不进维度图；成长**会**传到维度图

### ⑭ 本轮事故的共同教训

| 事故 | 漏掉它的原因 |
| --- | --- |
| `GEAR_SFX` 没转发 → 维度图打不开 | 测试只直接调 `drawRadar()`，没走完整渲染链 |
| `tabHistory()` 被误删 → 面板打不开 | 没有一项测试真的去渲染面板 |
| `fxReduced` 没转发 → 设置页打不开 | 同上 |
| `chartDims()` 漏成长 → 第二张图不成长 | 只测了 `dimsUpTo` 那条路径，没测这张图 |
| 多面转盘里打字存不住 | 渲染改成按面取选项了，**输入路径**忘了同步；"按 id 找选项"还有 6 处只查兜底面 |

**规律：不是"某个函数写错了"，而是"某条路径没人走过"。**
所以测试要盖住**调用链的最外层**（真实的入口 → 真实的视图 → 真实的绘制），
而不是只调内部小函数。每次都是靠这个思路补上测试之后，问题才真正被钉住。

**推论（多面转盘这次用上了）**：
- **同一件事有两处实现时**（两条数据路径 / 两套选项来源），**两条都要测**
  —— `dimsUpTo` vs `chartDims`、`wheelOptions` vs `faceOptions` 都是这类
- **数据从"一层"变成"多层"时**（`options` → 兜底面 + `faces[].options`），
  **所有按 id 定位对象的地方都要重查**：读、写、删、跳转、批量、复制、清空
- 新功能配套写测试，比事后补便宜得多：`t13`/`t14` 都是写的时候当场抓出问题的

---

## 5. 下一个任务（建议按序）

### ① 问用户：位置 vs 成长的语义（**需要他拍板**）
现在的行为：**成长只累积总量，不看发生在第几步**。
所以把维度图放在成长转盘**之前**，它的数值**也会**跟着涨（实测确认过）。

两种选择：
- **A. 保持现状**（简单）：图 = "本轮到目前为止的属性，含已获得的所有成长"。
  文档里写清楚就行。
- **B. 按位置定格**（更符合"位置"的直觉，但要动数据结构）：
  在 `flow.path` 的每一步记下当时的 `grow` 快照
  （`p.grow = JSON.parse(JSON.stringify(state.flow.grow))`），
  `dimsUpTo(anchorId)` 先找到 anchor 对应的那一步、用它那一刻的快照来算成长。
  ⚠️ 要注意：分支流程会来回跳，`path` 不是列表顺序；
  节点存档是 `version:3`，加字段要顺手写迁移。

### ② 真实浏览器里过一遍（最重要，30 秒）
打开网页 → 随便抽几次让流程走到维度图那一位。重点看：
- **维度图能不能正常显示**，两张图的形状**确实不一样**（成长生效）
- 「设置 / 记录」两个标签都能打开
- 转盘配色是不是「宣纸」的沉稳色（**如果又是鲜艳 → 色板注入又坏了**）
- 弹图 → 停留 → 自动收起 → 继续下一站；中途自己关掉图后面还继续走（不卡住）

出问题的话：`localStorage.setItem('zhoushu-wheel.debug','1')` → 刷新 →
控制台 `ZW.__app.openChart()`，然后看控制台的红色报错。

### ③ 补测试的习惯要落实
这轮四次事故全是"某条路径没人走过"。往后每加一个功能，
**先在 `_dev/` 写一项走真实入口的断言**（别只调内部小函数）。

### ④ 继续拆 chart / ui（可选，风险中等）
```
js/chart.js   等级解析 / 成长 / 维度图 / 法轮 / 雷达绘制
js/ui.js      NAV / 面板视图 / 列表 / 拖拽 / 事件委托 / 对话框
```
⚠️ 这两块和 `state` / `ui` 缠得最紧。**拆一块跑一次 `node _dev/run.js`**，
拆完再跑一次 `node _dev/symbolscan.js` 确认没漏转发。
⚠️ `chartDims` / `dimsUpTo` 这次刚因为"两条路径不一致"出过事 —— 拆的时候
把它们放在同一个文件里，别分家。

### ⑤ 小优化
- 「记录」页的维度图列表加缩略图
- 流程条显示「本站是维度图」的提示图标
- 维度图上标出"这一项涨了多少"（比如 `C（普通）↑2`），成长才看得见
- 旋转物理效果各存一套参数（现在 2D/3D 的圈数是写死的常量）

### ⑥ 想删的老东西
- `index.html.bak`（模块化之前的备份 —— **先别删！** 这轮 `tabHistory()` 就是从它里面捞回来的）
- `转盘/screenshot-*.png`、空的「分支转盘」文件夹、`.crswap` 临时文件
- `排查-维度图失效.md` 里的事已经做完，可以留档也可以删

---

## 6. 直接用这段开新对话

```
请先读 交接文档.md（在 C:\Users\chenh\Desktop\咒术转盘\ 下），然后按里面的「5. 下一个任务」开工。
我的偏好：纸墨手绘风、参数给成常量方便自己微调、不要引入外部依赖或打包工具（要双击 html 就能用）。
改完记得跑 node _dev/run.js 回归。
```
